<?php
    $this->headTitle('Formulários');

    $this->mainMenu()->setActiveItemId('quiz');
    $this->mainMenu()->setActiveListItemId('Formulários');

    $this->pageBreadcrumbs()
         ->setItems(['Inicio'=>$this->url('home'),
                     'Gerenciamento de Formulários'=>$this->url('quizForm')]);
?>
<div class="row">
    <?= $this->partial('cms/partial/alert'); ?>
</div>
<div class="row">
    <div class="col col-md-8">
        <h3>Gerenciamento de Formulário</h3>
    </div>
    <div class="col text-right">
        <a class="btn btn-sm btn-outline-primary rounded-circle hoverable-rotate hoverable-rotate-moviment" href="
            <?= $this->url('quizForm', ['action'=>'add']); ?>" title="Adicionar">
            <i class="fas fa-plus"></i>
        </a>
    </div>
</div>
<!-- name, form, order, required, status.and question_form_bond -->
<div class="row">
    <?= $this->partial('cms/partial/filter', ['search'=>$search, 'operators' => $operators]); ?>
</div>
<div class="table-responsive top-margin-10">
    <table class="table table-striped table-bordered table-hover dataTables">
        <thead class="thead-dark">
            <tr>
                <th>Nome</th>
                <th>Produto</th>
                <th>Status</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($forms as $quizForm): ?>
            <tr>
                <td>
                    <a href="<?= $this->url('quizForm', ['action'=>'view', 'id'=>$quizForm->getId()]); ?>">
                        <?= $this->escapeHtml($quizForm->getName()); ?>
                    </a>
                </td>
                <td>
                    <?= $quizForm->getIdProduto()->getNome(); ?>
                </td>
                <td>
                    <div class="status_string_<?= $quizForm->getId(); ?>">
                        <?= $this->escapeHtml($quizForm->getStatusAsString()); ?>
                    </div>
                    <div class="material-switch">
                        <input class="status-index" type="checkbox" name="status"
                               value="<?= $quizForm->getStatus() ;?>"
                               data-status="<?= $this->escapeHtml($quizForm->getStatusAsString()); ?>"
                               data-id="<?= $quizForm->getId(); ?>"
                               id="status_<?= $quizForm->getId(); ?>"
                               >
                        <label for="status_<?= $quizForm->getId(); ?>"></label>
                    </div>
                </td>
                <td>
                    <nobr>
                        <a class="btn btn-sm btn-info hoverable-scale"
                           href="<?= $this->url('quizForm', ['action'=>'edit', 'id'=>$quizForm->getId()]); ?>"
                           title="Editar">
                            <i class="far fa-edit"></i>
                        </a>
                        <a class="btn btn-sm btn-danger delete-action hoverable-scale"
                           data-id="<?= $quizForm->getId(); ?>"
                           href="#"
                           title="Remover">
                            <i class="fa fa-trash"></i>
                        </a>
                    </nobr>
                </td>
            </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
</div>

