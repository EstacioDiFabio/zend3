<?php
    $this->headTitle('Grupos');

    $this->mainMenu()->setActiveItemId('management');
    $this->mainMenu()->setActiveListItemId('Grupos');

    $this->pageBreadcrumbs()->setItems([
                'Home'=>$this->url('home'),
                'Gerenciamento de Grupos'=>$this->url('groups'),
                ]);
?>
<div class="row">
    <?= $this->partial('cms/partial/alert'); ?>
</div>
<div class="row">

    <div class="col">
        <h3>Gerenciamento de Grupos</h3>
    </div>
    <div class="col text-right">
        <a class="btn btn-sm btn-outline-primary rounded-circle hoverable-rotate hoverable-rotate-moviment" href="
            <?= $this->url('groups', ['action'=>'add']); ?>" title="Adicionar">
            <i class="fas fa-plus"></i>
        </a>
    </div>

</div>
<div class="row">
    <?= $this->partial('cms/partial/filter', ['search'=>$search, 'operators' => $operators]); ?>
</div>

<div class="table-responsive top-margin-10">
    <table class="table table-striped table-bordered table-hover dataTables">
        <thead class="thead-dark">
           <tr>
                <th>Nome</th>
                <th>Status</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($workGroups as $group): ?>
            <tr>
                <td>
                    <a href="<?= $this->url('groups', ['action'=>'view', 'id'=>$group->getId()]); ?>">
                        <?= $this->escapeHtml($group->getName()); ?>
                    </a>
                </td>
                <td>
                    <div class="status_string_<?= $group->getId(); ?>">
                        <?= $this->escapeHtml($group->getStatusAsString()); ?>
                    </div>
                    <div class="material-switch">
                        <input class="status-index" type="checkbox" name="status"
                               value="<?= $group->getStatus() ;?>"
                               data-status="<?= $this->escapeHtml($group->getStatusAsString()); ?>"
                               data-id="<?= $group->getId(); ?>"
                               id="status_<?= $group->getId(); ?>"
                               >
                        <label for="status_<?= $group->getId(); ?>"></label>
                    </div>
                </td>
                <td>
                    <a class="btn btn-sm btn-info hoverable-scale"
                       href="<?= $this->url('groups', ['action'=>'edit', 'id'=>$group->getId()]); ?>"
                       title="Editar">
                        <i class="far fa-edit"></i>
                    </a>
                    <a class="btn btn-sm btn-danger delete-action hoverable-scale"
                       data-id="<?= $group->getId(); ?>"
                       href="#"
                       title="Remover">
                        <i class="fa fa-trash"></i>
                    </a>
                </td>
            </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
</div>
